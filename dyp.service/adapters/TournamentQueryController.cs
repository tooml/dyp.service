using dyp.contracts.messages.queries.tournament;
using servicehost.contract;
using System;

namespace dyp.service.adapters
{
    [Service]
    public class TournamentQueryController
    {
        //public static Func<ITournamentQueryHandling> _tournamentQueryHandling;

        //[EntryPoint(HttpMethods.Get, "/api/v1/turnier")]
        //public TournamentQueryResult Load_turnier(string id)
        //{
        //    Console.WriteLine($"turnier laden: { id }");
        //    return _tournamentQueryHandling().Handle(new TournamentQuery() { Id = id });
        //}
    }
}
